#include<iostream>
#include<stack>
#include<vector>

using namespace std;

int main(){
    int t; cin >> t;
    while(t--){
        int N, K; cin >> N >> K;

        vector<long> arr(N);
        for(int i = 0; i < N; i++) cin >> arr[i];

        
    }

    return 0;
}